/** GML array functions. */

// TODO: re-verify against spcs
export function array_length(arr: unknown[]): number { return arr.length; }

// TODO: re-verify against spcs
export function array_length_1d(arr: unknown[]): number { return arr.length; }

// TODO: re-verify against spcs
export function array_contains(arr: unknown[], value: unknown): boolean { return arr.includes(value); }
